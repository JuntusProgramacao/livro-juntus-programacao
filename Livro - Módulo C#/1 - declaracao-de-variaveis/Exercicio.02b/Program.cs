// b) X ← 12.0;
// X ← X + 2.0 * 3.0;
// Y ← 5.0;
// Z ← 6.3;
// A ← 12.98;
// A ← A + Y;
// Z ← X * 2 – (Z + Y);
// K ← 2.6 + A;
// K ← K * K;
// Z ← Z + 2.5 * A;
// X ← X / 2.0 * 3.5 + ((A * 3.0) – Y * 2);
// Y ← X + Y + Z + A;

double X, Y, Z, A, K;

X = 12.0;
X += 2.0 * 3.0;
Y = 5.0;
Z = 6.3;
A = 12.98;
A += Y;
Z = (X * 2) - (Z + Y);
K = 2.6 + A;
K = K * K;
Z += 2.5 * A;
X = (X / 2.0 * 3.5) + ((A * 3.0) - (Y * 2));
Y = X + Y + Z + A;
